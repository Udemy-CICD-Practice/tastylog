name: "Infra Preview Deploy"

on:
  push:
    branches:
      - main
    paths:
      - "10_infra/**"
      - ".github/workflows/**"
permissions:
  id-token: write
  contents: write


jobs:
  preview:
    name: "preview"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"
